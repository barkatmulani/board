import { EDirection } from "./models";

export const maxX: number = 4;
export const maxY: number = 4;
export const DIRECTIONS: EDirection[] = [EDirection.NORTH, EDirection.EAST, EDirection.SOUTH, EDirection.WEST];